# HW#2. Termial ![Terminal](https://img.shields.io/badge/GitBash-5.2.15(1)-f14e32) ![Editor](https://img.shields.io/badge/VScode-1.81.1-0071bc) ![OS](https://img.shields.io/badge/Windows-20H2-0014a8)


*1) Сделать папку dir_1* : 

`mkdir dir_1`

___
*2) Зайти в папку dir_1* : 

`cd dir_1`
___
*3) Создать папку inner_dir_1* : 

`mkdir inner_dir_1`
___
*4) Посмотреть где ты находишься* : 

`pwd`
___
*5) Находясь в папке dir_1 создать пустой текстовый файл tf_1.txt* : 

`touch tf_1.txt`
___
*6) Находясь в папке dir_1 через команду cat создать текстовый файл tf_2.txt со следующими строками: the first 1, the second 2, the third 3* :
```git
cat > tf_2.txt
```
*Вводим следующие строки в файл:*
```git
the first 1
the second 2
the third 3 
```
+ Для выхода из команды сначала используем **enter** для перехода на новую строку, затем сочетание клавиш **ctrl+c**
___
*7) Зайти в папку inner_dir_1* : 

`cd inner_dir_1`
___
*8) Через cat сделать текстовый файл tf_3.txt  c любыми строками* :
```git
cat > tf_3.txt
```
*Вводим следующие строки в файл:*
``` 
123
456
789
```
+ Для выхода из команды сначала используем **enter** для перехода на новую строку, затем сочетание клавиш **ctrl+c**
___
*9) Через cat добавить в текстовый файл tf_3.txt строку “the second 2”* :
```git
cat >> tf_3.txt
```
*Вводим следующие строки в файл:*
```git
the second 2
```
+ Для выхода из команды сначала используем **enter** для перехода на новую строку, затем сочетание клавиш **ctrl+c**
___
*10) Через cat добавить в текстовый файл tf_3.txt строку “the sec 2”* : 
```git
cat >> tf_3.txt
```
*Вводим следующие строки в файл:*
```git
the sec 2 
```
+ Для выхода из команды сначала используем **enter** для перехода на новую строку, затем сочетание клавиш **ctrl+c**
___
*11) Через cat добавить в текстовый файл tf_2.txt строку “the sec 3”* :  
```git
cat >> ../tf_2.txt
```
*Вводим следующие строки в файл:*
```git 
the sec 3 
```
+ Для выхода из команды сначала используем **enter** для перехода на новую строку, затем сочетание клавиш **ctrl+c**
___
*12) Через cat добавить в текстовый файл tf_3.txt строку “the SeCoNd 2”* : 
```git
cat >> tf_3.txt  
```
*Вводим следующие строки в файл:*
```git
the SeCoNd 2
```
+ Для выхода из команды сначала используем **enter** для перехода на новую строку, затем сочетание клавиш **ctrl+c**
___
*13) Через cat добавить в текстовый файл tf_2.txt строку “the seConD 2”* : 
```git
cat >> ../tf_2.txt
```
*Вводим следующие строки в файл:*
```git
the seConD 2 
```
+ Для выхода из команды сначала используем **enter** для перехода на новую строку, затем сочетание клавиш **ctrl+c**
___
*14) Сделать текстовый файл tf_4.txt в котором будет 15 строк* :
```git
cat >> tf_4.txt
```
*Вводим следующие строки в файл:*
```git
line 1
line 2
line 3
line 4
line 5
line 6
line 7 
line 8
line 9
line 10
line 11
line 12
line 13
line 14
line 15
```
+ Для выхода из команды сначала используем **enter** для перехода на новую строку, затем сочетание клавиш **ctrl+c**
___

*15) Сделать текстовый файл tF_5.txt в котором будет 13 строк* :
```git
cat > tF_5.txt
```
*Вводим следующие строки в файл:*
```git
line 1
line 2
line 3
line 4
line 5
line 6
line 7 
line 8
line 9
line 10
line 11
line 12
line 13
```
+ Для выхода из команды сначала используем **enter** для перехода на новую строку, затем сочетание клавиш **ctrl+c**
___
*16) Вывести список всех файлов в папке* : 

`ls -la`
___
*17) Выйти из папки inner_dir_1* : 

`cd ..`
___
*18) Вывести содержимое файла tf_3.txt в терминал* : 

`ls -Rla` - чтобы посмотреть где находится файл tf_3.txt
`cat inner_dir_1/tf_3.txt`
___
*19) Найти путь к файлу tf_4.txt* : 

`find . -name 'tf_4.txt'`
___
*20) Отчистить файл tf_4.txt от содержимого без удаления самого файла* :
+ Вариант 1:

  `cat > inner_dir_1/tf_4.txt`
  + Для выхода из команды используем сочетание клавиш **ctrl+c**
+ Вариант 2:

  `vim inner_dir_1/tf_4.txt`
  + Нажимаем кнопку **i** для активации режима _insert_, при помощи клавиш :arrow_up: :arrow_down: :arrow_left: :arrow_right: и **backspace** стираем весь текст, нажимаем **esc**, затем использем **shift+6** ( для написания символа ":") после двоеточия пишем **wq** чтобы сохранить и выйти из редактора. 
___
*21) Найти путь к файлам у которых есть  “tf” в названии* : 

`find . -name "*tf*"`
___
*22) Найти путь к файлам у которых есть  “tf” в названии и буквы в любом регистре* : 

`find . -iname "*tf*"`
___
*23) Найти строки в файлах где есть комбинация букв “sec” в текущей папке* : 

`grep 'sec' *`
___
*24) Найти строки в файлах где есть комбинация букв “sec” в любом регистре в текущей папке* : 

`grep -i 'sec' *`
___
*25) Найти строки в файлах где есть только комбинация букв “sec” в текущей папке* :

  `grep -w sec *`
___
*26) Найти строки в файлах где есть только комбинация букв “sec” в любом регистре в текущей папке* : 

`grep -w -i 'sec' *`
___
*27) Найти строки в файлах где есть комбинация букв “second” в текущей папке* : 

`grep 'second' *`
___
*28) Найти строки в файлах где есть комбинация букв “second” в любом регистре в текущей папке* :

`grep -i 'second' *`
___
*29) Найти строки в файлах где есть комбинация букв “second” во всех папках ниже уровнем* : 

`grep -r 'second' *`
___
*30) Найти только путь и название файла в строках которых есть комбинация букв “second” в текущей папке* : 

`grep -l 'second' *`
___
*31) Найти все строки во всех файлах где нет комбинации “second”* : 

`grep -r -v 'second' .`
___
*32) Найти только название и путь к файлам где нет комбинации “second”* : 

`grep -r -l -v 'second' .`
___
*33) Вывести в терминал 4 последних строк любого текстового файла* :

`tail -n4 inner_dir_1/tF_5.txt`
___
*34) Вывести в терминал 4 первые строки любого текстового файла* :

`head -n4 inner_dir_1/tF_5.txt`
___
*35) Команда в одну строку. Создать папку и создать текстовый файл с содержиммым* :

`mkdir new_folder && cat > tf_6.txt`
*Вводим следующие строки в файл:*
```git
sec
Sec
SEC
second
Second
SECOND
```
+ Для выхода из команды сначала используем **enter** для перехода на новую строку, затем сочетание клавиш **ctrl+c**

___
*36) Команда в одну строку. Переместить в любую одну папку текстовые файлы у которых в содержимом есть слово “sec”* : 

`grep -r -l -w sec | xargs mv -t new_folder/`
___
*37) Команда в одну строку. Скопировать в любую одну папку текстовые файлы у которых в содержимом есть слово “sec”* : 

`mkdir new_folder_2 && grep -l -r sec * | xargs cp -t new_folder_2/`
___
*38) Команда в одну строку. Найти все строки c “sec” во всех текстовых файлах, скопировать и вставить эти строки в один новый созданный текстовый файл* : 

`grep -r -w sec * > tf_7.txt`
___
*39) Команда в одну строку. Удалить текстовые файлы у которых в содержимом есть слово “sec”* : 

`grep -r -l -w sec  | xargs rm`
___
*40) Просто вывести в терминал строку “Good job!!”* : 

`echo 'Good job!!'`
___
